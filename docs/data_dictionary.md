# Data dictionary

The 20 original South German Credit predictors are preserved as documented integer-coded fields. `target=1` means the original `kredit=0` bad outcome. Synthetic LGD, EAD, and longitudinal data are stored separately and always carry `synthetic=true`.

